sudo: required

services: docker

before_install:
  - docker-compose up --build -d

script:
  - domcer-compose exec --env 'RAILS_ENV=test' web rails db:create
  - domcer-compose exec --env 'RAILS_ENV=test' web rails db:migrate
  - domcer-compose exec --env 'RAILS_ENV=test' web rails test